'use strict';

let React = require('react-native');

const {
	SCREEN_WIDTH
} = require('./variables');

module.exports = React.StyleSheet.create({
	container: {
		height: 135
	},

	image: {
		height: 135
	}
});
'use strict';

let React = require('react-native');

const {
	PADDING_S_SIZE
} = require('./variables');

module.exports = React.StyleSheet.create({
	container: {
		flex: 1
	},

	list: {
		flex: 1
	}
});